
name: school_fixed
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  gsheets: ^0.4.2
  intl: ^0.18.1

flutter:
  uses-material-design: true
  assets:
    - assets/credentials.json
